declare namespace chrome {
  namespace tabs {
    interface Tab {
      url?: string;
      // otras propiedades que necesites
    }
    // otras interfaces y tipos relacionados con la API de chrome.tabs
  }
  // otros espacios de nombres y tipos relacionados con la API de Chrome
}
